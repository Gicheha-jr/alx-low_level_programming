#include "main.h"

/**
 * _isupper - Check if a letter is upper
 * @x: The number to be checked
 * Return: 1 for upper letter or 0 if any else
 */

int _isupper(int x)

{
	if (x >= 48 && x <=  57)
	{
	return (1);
	}
	return (0);
}
